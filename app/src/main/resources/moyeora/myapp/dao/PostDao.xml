<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="moyeora.myapp.dao.PostDao">


  <resultMap id="PostMap" type="Post">
  <id column="post_no" property="no"/>
  <result column="school_no" property="schoolNo"/>
  <result column="user_no" property="userNo"/>
  <result column="category_no" property="categoryNo"/>
  <result column="content" property="content"/>
  <result column="created_at" property="createdAt"/>

  <association property="userNo" javaType="User">
    <id column="user_no" property="no"/>
    <result column="name" property="name"/>
  </association>

  <collection property="fileList" ofType="AttachedFile">
    <id column="file_no" property="no"/>
    <result column="file_path" property="filePath"/>
    <result column="post_no" property="postNo"/>
  </collection>
</resultMap>

  <insert id="add" parameterType="Post"
    useGeneratedKeys="true" keyColumn="no" keyProperty="no">
    insert into posts(school_no,user_no,category_no,created_at)
    values(#{schoolNo},#{userNo.no},#{categoryNo},#{createdAt})
  </insert>

  <delete id="delete" parameterType="int">
    delete from posts
    where no=#{value}
  </delete>

  <select id="findAll" resultMap="PostMap">
    select
      p.no,
      p.school_no,
      p.user_no,
      p.category_not,
      p.created_at
    from
      posts p left inner join users u on p.user_no = u.user_no
    where
      p.categoryNo=#{categoryNo}
    order by
      p.no desc
    limit #{rowCount} offset #{offset}
  </select>

  <select id="findBy" resultMap="PostMap" parameterType="int">
    select
      p.no,
      p.school_no,
      p.user_no,
      p.category_notice,
      p.category_regular,
      p.created_at
    from
      posts p inner join user u on p.user_no = u.user_no
    where
      p.no=#{value}
  </select>

  <update id="update" parameterType="Post">
    update posts set
      school_no=#{schoolNo},
      user_no=#{userNo.no},
      category_no=#{categoryNo},
    where
      no=#{no}
  </update>

  <select id="countAll" resultType="int" parameterType="int">
    select count(*) from posts where categoryNo=#{value}
  </select>
</mapper>